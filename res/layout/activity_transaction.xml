<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/txtVDashbord"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".TransactionActivity" >

    <ListView
        android:id="@+id/listVTransactions"
        android:layout_width="wrap_content"
        android:layout_height="263dp"
        android:fadingEdge="none"
        android:layout_above="@+id/txtVRequestStatus"
        android:layout_alignLeft="@+id/txtVRequestStatus"
        android:layout_alignParentTop="true" >
    </ListView>

    <Button
        android:id="@+id/btnAddTransaction"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/txtVRequestStatus"
        android:layout_alignParentBottom="true"
        android:text="@string/add" />

    <TextView
        android:id="@+id/txtVRequestStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnAddTransaction"
        android:layout_centerHorizontal="true"
        android:paddingBottom="5dp"
        android:paddingTop="5dp"
        android:text="@string/results"
        android:textAppearance="?android:attr/textAppearanceMedium" />

</RelativeLayout>